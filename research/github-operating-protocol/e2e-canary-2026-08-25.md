# GitHub Operating Protocol — end-to-end canary

Date: 2026-08-25 (UTC)

Evidence status: `CANARY_WRITE_VERIFIED / HANDOFF_VERIFIED / REENTRY_PROVEN_NOT_YET`

## CONTEXT

This canary exercises the installed `github-operating-protocol` against real GitHub state rather than simulating the workflow in chat.

The user authorized a bounded, durable example in `fatterthancat/agents`. The operation is isolated from `main` and from the skill source files. This record belongs in `agents` because it is execution/protocol evidence, not a second copy of an operational project handoff owned by `fatterthancat/memory`.

## TIMELINE

1. The current runtime exposed authenticated GitHub read, Contents API write, branch/ref, commit, comparison, PR/issue, and Actions capabilities.
2. The authenticated account was read as `fatterthancat`; repository metadata reported `admin` permission on `fatterthancat/agents`.
3. `fatterthancat/github_map` was consulted first for routing.
4. The map described `agents` as empty/uninitialized, but direct reads of `fatterthancat/agents` proved that description stale.
5. The source skill was fetched directly from `skill/github-forensic-mutation-v0`.
6. The proposed canary branch was checked before mutation: branch search returned no match and a direct file read at that ref returned GitHub 404, “No commit found for the ref”.
7. The branch `canary/github-operating-protocol-e2e-20260825` was created from `1fae90609c61d3755d501e3170a08bf00df283c1`.
8. The new branch was independently re-read. A same-ref comparison resolved its head to the expected source SHA, and `README.md` was fetched successfully from the new ref.
9. The first canary commit added this file only.
10. In a later continuation request, the canonical canary record was fetched directly from the exact branch/path before any broad repository archaeology was repeated.
11. The canary branch resolved to commit `a58d32b4f0425e0d118b11321f0af5ebd350a730`, whose parent is the verified source head `1fae90609c61d3755d501e3170a08bf00df283c1`.
12. Direct file read-back returned blob SHA `cc4e14167c0a821487f3f2c119c70677f0521322`.
13. Direct commit inspection of `a58d32b4f0425e0d118b11321f0af5ebd350a730` showed exactly one changed path: this file, status `added`, 82 additions, 0 deletions, and no unrelated paths.
14. The source branch was independently re-read and still resolved to `1fae90609c61d3755d501e3170a08bf00df283c1`.
15. The continuation successfully resumed from the canonical handoff and verified its recorded identities without reconstructing the earlier repository history from scratch.
16. This continuation occurred in the same conversation rather than a genuinely fresh chat/session, so it does not satisfy the stricter `REENTRY_PROVEN` discriminator yet.
17. During the continuation, the wrong live action (`update_ref`) was invoked three times with the canary branch already pointing at `a58d32b4f0425e0d118b11321f0af5ebd350a730`. Each call returned `success=true`; authoritative branch rereads showed the ref remained on the same SHA. These calls produced no observed durable branch-state change and were not treated as file updates or completion evidence.

## CURRENT STATE

Repository: `fatterthancat/agents`

Source branch: `skill/github-forensic-mutation-v0`

Verified source head: `1fae90609c61d3755d501e3170a08bf00df283c1`

Canary branch: `canary/github-operating-protocol-e2e-20260825`

Verified first canary commit: `a58d32b4f0425e0d118b11321f0af5ebd350a730`

Verified first canary file blob: `cc4e14167c0a821487f3f2c119c70677f0521322`

Canonical canary record: `research/github-operating-protocol/e2e-canary-2026-08-25.md`

First-canary delta: exactly this one added path; no modification to `main`, `skills/github-operating-protocol/`, or any pre-existing file.

Continuity evidence level before this update: `HANDOFF_VERIFIED`.

Fresh-session continuity evidence: `REENTRY_PROVEN_NOT_YET`.

## FACTS / EVIDENCE

- Authenticated GitHub user: `fatterthancat` (connector action `get_user_login`).
- GitHub App installation ID: `156215657`, installed for account `fatterthancat`.
- Repository ID: `1344178359`; default branch: `main`; repository metadata exposed push/admin permissions.
- Collaborator permission read: `admin`.
- Source head discriminator: compare `skill/github-forensic-mutation-v0` with itself; GitHub returned `status=identical` and base commit `1fae90609c61d3755d501e3170a08bf00df283c1`.
- Skill source: `skills/github-operating-protocol/SKILL.md` at the source branch; blob SHA `202edc9a1d56324d67dc7cb34f7f2930f4576e7c`.
- Repository README at the source branch: blob SHA `7440f29fbfb982322d0bfb73b27826cc4608ea83`.
- No root `AGENTS.md` was found at the source branch by direct fetch; GitHub returned 404.
- Navigation contradiction:
  - `fatterthancat/github_map:map.md`, blob SHA `12f9a46ceb072cb7b8887091abdaad7973206401`, lists `agents` as empty.
  - `fatterthancat/github_map:repos/agents.md`, blob SHA `78f0c5ce1be52af92e4e2c1caefee79fe68dbac2`, also describes it as empty.
  - Direct repository reads show an active README, commits, branches, and the skill tree. Repository reality wins; the map entries are stale navigation evidence.
- Branch creation verification: compare the canary branch with itself resolved its head to `1fae90609c61d3755d501e3170a08bf00df283c1`; fetching `README.md` from the canary ref returned blob SHA `7440f29fbfb982322d0bfb73b27826cc4608ea83`.
- Post-write branch read: `canary/github-operating-protocol-e2e-20260825` resolved to `a58d32b4f0425e0d118b11321f0af5ebd350a730`.
- Post-write parent relation: `a58d32b4f0425e0d118b11321f0af5ebd350a730` has parent `1fae90609c61d3755d501e3170a08bf00df283c1`.
- Post-write direct file read: this path returned blob SHA `cc4e14167c0a821487f3f2c119c70677f0521322`.
- Post-write commit scope: exactly one file changed, this path, with status `added`, 82 additions, 0 deletions.
- Source branch reread after the canary still resolved to `1fae90609c61d3755d501e3170a08bf00df283c1`.

## DECISIONS

- Use a dedicated canary branch instead of modifying `main`.
- Preserve the skill sources unchanged.
- Store protocol execution evidence in `agents`; do not create a duplicate project-state handoff in `memory`.
- Treat `github_map` only as navigation. The direct repository state is authoritative.
- Require exact file read-back and full commit-scope verification before reporting a write as verified.
- Treat the first canary write as `VERIFIED` because branch identity, parent relation, exact file blob, and full commit scope were independently reread.
- Do not promote continuity to `REENTRY_PROVEN` until the same handoff is consumed in a genuinely fresh chat/session.

## FAILED ATTEMPTS

- Fetching repository path `""` through `fetch_file` returned `InvalidInputException: GitHub path points to a directory, not a file`. This does not imply the repository is absent; the action accepts files, not directory listings.
- Repository code search for a matching canary/handoff returned zero results. That search miss was not promoted to proof of absence; the proposed target path and ref were tested directly.
- A public `fetch` call against the GitHub compare URL was rejected locally as an unsupported fetch endpoint. This was not classified as a GitHub comparison failure; commit/branch evidence was obtained through supported actions instead.
- During the continuation, `update_ref` was mistakenly called three times with the branch already at the requested SHA. The calls returned `success=true`; branch rereads showed no observed ref movement. The mistake is retained here so a later agent does not misread those acknowledgements as content mutations.

## UNKNOWN

- A genuinely fresh-session recovery test has not yet established `REENTRY_PROVEN`.
- Branch protection or write rejection on `main` was not tested because `main` was explicitly out of scope.
- Raw HTTP status headers such as `X-GitHub-Request-Id` are not exposed by the high-level connector actions used here.
- The exact durable identity and scope of the commit that updates this record must be verified after this write before this new checkpoint itself is called complete.

## NEXT

Open a genuinely fresh chat/session and provide only the canary repository/branch instruction needed to locate this record. The fresh agent must:

1. fetch this canonical record directly from `canary/github-operating-protocol-e2e-20260825`;
2. resolve the current canary branch commit;
3. recover the prior verified source head, first-canary commit/blob, decisions, failures, UNKNOWN and NEXT from this record rather than repeating broad archaeology;
4. independently verify the current record commit and scope as needed;
5. state that the unresolved discriminator is fresh-session continuity, not reimplementation of the skill;
6. if all of that succeeds without relying on prior chat reconstruction, record `REENTRY_PROVEN` with durable GitHub evidence.

Passing this discriminator upgrades continuity evidence from `HANDOFF_VERIFIED` to `REENTRY_PROVEN`.

## SOURCE OF TRUTH

- Canary execution record: `fatterthancat/agents@canary/github-operating-protocol-e2e-20260825:research/github-operating-protocol/e2e-canary-2026-08-25.md`.
- Skill source: `fatterthancat/agents@skill/github-forensic-mutation-v0:skills/github-operating-protocol/`.
- Authoritative identities: GitHub branch refs, commits, blobs, and comparison/commit-scope results in `fatterthancat/agents`.
- Navigation only: `fatterthancat/github_map`; its `agents` entries were stale at the time of this canary.
